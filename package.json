{
  "name": "@better-vibe/branch-narrator",
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "version": "1.5.0",
  "description": "A local-first CLI that reads git diff and generates structured PR descriptions",
  "type": "module",
  "bin": {
    "branch-narrator": "./dist/cli.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rm -rf dist && bun build ./src/cli.ts ./src/index.ts --outdir ./dist --target node --packages external --sourcemap && tsc --emitDeclarationOnly --declaration --outDir dist",
    "dev": "bun build ./src/cli.ts ./src/index.ts --outdir ./dist --target node --packages external --sourcemap --watch",
    "test": "bun test tests/*.test.ts --timeout 10000",
    "test:e2e": "bun test tests/e2e/*.test.ts --timeout 60000",
    "test:watch": "bun test tests/*.test.ts --watch --timeout 10000",
    "test:benchmarks": "bun test benchmarks/ --timeout 30000",
    "test:all": "bun run test && bun run test:e2e",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "bun run build",
    "changeset:add": "changeset add --open",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun run build && changeset publish",
    "benchmark": "bun benchmarks/run.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "git",
    "diff",
    "pr",
    "pull-request",
    "cli",
    "sveltekit",
    "react",
    "react-router",
    "supabase",
    "cloudflare"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@babel/parser": "^7.26.3",
    "@babel/traverse": "^7.26.5",
    "@babel/types": "^7.26.3",
    "boxen": "^8.0.1",
    "chalk": "^5.6.2",
    "cli-table3": "^0.6.5",
    "commander": "^12.1.0",
    "execa": "^9.5.2",
    "ora": "^9.0.0",
    "parse-diff": "^0.11.1",
    "picomatch": "^4.0.3",
    "semver": "^7.6.3"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@types/babel__traverse": "^7.20.6",
    "@types/bun": "^1.3.5",
    "@types/node": "^20.17.10",
    "@types/picomatch": "^4.0.2",
    "@types/semver": "^7.5.8",
    "typescript": "^5.7.2"
  }
}
